<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="smartagora.ethz.ch"
    tools:ignore="GoogleAppIndexingWarning">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_WIMAX_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- <uses-permission android:name="android.permission-group.LOCATION"/> -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="ch.ethz.coss.smartagora.hub.BIND_PERM" />
    <uses-permission
        android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ProtectedPermissions" />

    <uses-feature android:name="android.hardware.location.gps" />
    <permission
        android:name="smartagora.ethz.ch.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="smartagora.ethz.ch.permission.MAPS_RECEIVE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <application
        android:name="smartagora.ethz.ch.Application"
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup"
        android:usesCleartextTraffic="true"
        >
        <uses-library android:name="org.apache.http.legacy" android:required="false"/>

        <!-- //android:name="android.support.multidex.MultiDexApplication" -->

        <activity
            android:name="smartagora.ethz.ch.uiActivities.QuestionsActivity"
            android:noHistory="false"
            android:theme="@style/Theme.AppCompat.Dialog"
            tools:ignore="InnerclassSeparator" />
        <activity
            android:name="smartagora.ethz.ch.uiActivities.ProjectsActivity"
            android:noHistory="false"
            android:theme="@style/Theme.AppCompat.Dialog"
            tools:ignore="InnerclassSeparator" />
        <activity
            android:name="smartagora.ethz.ch.uiActivities.HelpActivity"
            android:noHistory="false"
            android:theme="@style/Theme.AppCompat.Dialog"
            tools:ignore="InnerclassSeparator" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDuHVpo-FSrVEegiVZboarG-BU8GzGqhIg" />

        <service
            android:name="smartagora.ethz.ch.services.LocationService"
            android:enabled="true" />

        <activity
            android:name="smartagora.ethz.ch.uiActivities.Splash"
            android:theme="@style/AppTheme.NoActionBar"
            tools:ignore="InnerclassSeparator">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="smartagora.ethz.ch.uiActivities.HomeActivity"
            android:label="@string/title_activity_home_screen3"
            android:theme="@style/AppTheme.NoActionBar"
            tools:ignore="InnerclassSeparator"
            android:screenOrientation="portrait"
            android:configChanges="orientation|keyboardHidden" />
        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="88fd2c8b2052e2115223a4315e9c121d2f09e1e7" />

    </application>

</manifest>